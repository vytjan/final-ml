THE PLAN:

1. DESKEWING (preprocessing).
	If we deskew the image (align horizontally) then the algorithm must not learn that "skewed" variation of images.
	Image moments are used for that:
		HOG - histogram of gradients
		SVM - support vector machine
		
		To remove inner contours from everywhere.

learn31.png
learn32.png
learn41.png